import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static export for deployment flexibility
  // output: "export", // Uncomment for static hosting (non-Vercel)
};

export default nextConfig;
